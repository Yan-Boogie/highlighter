import React, { useRef, useImperativeHandle } from 'react';
import isEqual from 'fast-deep-equal';

export interface ISlateHighlighter {
  children: React.ReactNode;
}

const SlateHighlighter = (props: ISlateHighlighter) => {
  const {} = props;

  const 
};

export default SlateHighlighter;
